@inherits InputBaseComponent<DateValueModel>

<InputControl Model="Model" Label="@Label" IsRequired="@IsRequired">
    <InputField>
        <BasicInputDate CssClass="" Value="@context.Value" ValueChanged="@((value) => context.Value = value)" />
        @if (IsRequired)
        {
            <ValidationMessage For="@(() => context.Value)" />
        }
    </InputField>
</InputControl>